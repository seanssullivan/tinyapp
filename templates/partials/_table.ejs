<h3>My URLs</h3>
  <table class="table">
    <thead>
      <tr>
        <th scope="col">Short URL</th>
        <th scope="col">Long URL</th>
        <th scope="col">Total Clicks</th>
        <th scope="col">Unique Clicks</th>
        <th scope="col">Edit</th>
        <th scope="col">Delete</th>
      </tr>
    </thead>
    <tbody>
      <% for(let url of urls) { %>
        <tr>
          <td><%= url.shortURL %></td>
          <td><%= url.longURL %></td>
          <td><%= url.totalClicks %></td>
          <td><%= url.uniqueClicks %></td>
          <td><a class="btn btn-outline-primary btn-sm" href="/urls/<%= url.shortURL %>">Edit</a></td>
          <td>
            <form action="/urls/<%= url.shortURL %>/delete" method="POST">
              <button class="btn btn-outline-danger btn-sm">Delete</input>
            </form>
          </td>
        </tr>
      <% }%>
    </tbody>
  </table>